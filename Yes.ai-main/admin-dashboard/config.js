// admin-dashboard/config.js
// Fill these in once your Supabase project exists (Settings → API).
// SUPABASE_ANON_KEY is the PUBLIC "anon" key — safe to expose in browser
// code, because Row Level Security (see database/schema.sql) is what
// actually restricts what each logged-in user can see. Never put the
// service_role key here.
//
// Leave both blank to keep this dashboard running in DEMO MODE
// (local browser data, no real login required).

window.YESAI_CONFIG = {
  SUPABASE_URL: 'https://wrwbslqnqgdkmlrfaojw.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_xUQ45iNy66RkVYnwV2IxbA_92WsiWHn'
};
